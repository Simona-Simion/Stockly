
[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = [
    "chmod +x mvnw",
    "./mvnw -B -DskipTests clean package"
]

[start]
cmd = "java -Dserver.port=$PORT $JAVA_OPTS -jar target/*jar"