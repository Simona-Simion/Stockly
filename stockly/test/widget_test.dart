// Test de humo básico generado por Flutter. Se mantiene desactivado porque
// la app necesita conexión a la API para funcionar (no tiene mocks).
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test placeholder', (WidgetTester tester) async {
    // Tests de integración se añadirán en Fase 3
    expect(true, isTrue);
  });
}
